const glasses = state => state.glasses
const shapesFilter = state => state.shapesFilter;
const colorsFilter = state => state.colorsFilter;
const collections = state => state.collections;
const currentCollection = state => state.currentCollection

export default {
  glasses,
  shapesFilter,
  colorsFilter,
  collections,
  currentCollection
}